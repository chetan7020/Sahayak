<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Background color -->
    <corners android:radius="16dp" /> <!-- Corner radius for rounded edges -->
    <stroke
        android:width="1dp"
        android:color="@color/white" /> <!-- Border color and width -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" /> <!-- Padding inside the shape -->
</shape>
